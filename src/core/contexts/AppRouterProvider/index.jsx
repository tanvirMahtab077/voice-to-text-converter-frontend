/* eslint-disable react/prop-types */
import { RouterProvider } from 'react-router-dom';

function AppRouterProvider({ router }) {
	return <RouterProvider router={router} />;
}

export default AppRouterProvider;
